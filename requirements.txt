# FaceAnything inference requirements (tested versions; Python 3.11).
#
# torch / xformers below are the CUDA 12.8 builds we tested. Install torch first
# from the matching CUDA index, e.g.:
#   pip install torch==2.9.0 torchvision==0.24.0 --index-url https://download.pytorch.org/whl/cu128
# then `pip install -r requirements.txt` for the rest.

torch==2.9.0
torchvision==0.24.0
xformers==0.0.33.post1

numpy==1.26.4
scipy==1.16.3
einops==0.8.1
omegaconf==2.3.0
huggingface-hub==1.1.7
safetensors==0.7.0
e3nn==0.5.8

opencv-python==4.11.0.86
pillow==12.0.0
imageio==2.37.2
imageio-ffmpeg==0.6.0
trimesh==4.10.0
plyfile==1.1.3
tqdm==4.67.1
matplotlib==3.10.7
gdown==5.2.0
